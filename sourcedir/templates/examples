--------
Examples
--------

.. include:: examples/intro.rst

{% for ds in ds_list %}

.. |collapse{{ ds | regex_replace ("[^A-Za-z0-9]","") }}| collapse:: structure
   
   .. literalinclude:: examples/{{ ds }}.json
       :language: json

Input **{{ ds | upper }}** \[ |collapse{{ ds | regex_replace ("[^A-Za-z0-9]","") }}| \]

.. csv-table::
   :file: examples/{{ ds }}.csv
   :header-rows: 1
{% endfor %}

{% for example in ex_list %}
^^^^^^^^^
Example {{ example.i }}
^^^^^^^^^
.. literalinclude:: examples/{{ example.name }}.vtl

.. |collapse{{ example.name | regex_replace ("[^A-Za-z0-9]","") }}| collapse:: structure
   
   .. literalinclude:: examples/{{ example.name }}.json
       :language: json

Result **{{ example.name | upper }}** \[ |collapse{{ example.name | regex_replace ("[^A-Za-z0-9]","") }}| \]

.. csv-table::
   :file: examples/{{ example.name }}.csv
   :header-rows: 1
{% endfor %}
